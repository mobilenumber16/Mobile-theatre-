
# MobileTheatre Backend (Node.js)
Run the following commands:

1. Install Node.js if not already installed.
2. Open terminal inside the folder and run:
   npm install
   npm start

This will launch a basic API backend.
